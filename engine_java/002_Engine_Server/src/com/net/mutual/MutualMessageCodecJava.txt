package //package;

import java.io.IOException;
import com.net.mutual.MutualMessage;
import com.net.mutual.MutualMessageCodec;
import com.net.NetDataInput;
import com.net.NetDataOutput;
import com.net.NetDataTypes;
//import

/**
 * 此代码为自动生成。不需要在此修改。若有错误，请修改代码生成器。
 */
public class //className implements MutualMessageCodec
{
	public String getVersion() {
		return "//version";
	}

	public void readMutual(MutualMessage msg, NetDataInput in) throws IOException 
	{
//readExternal
	}

	public void writeMutual(MutualMessage msg, NetDataOutput out) throws IOException 
	{
//writeExternal
	}

//classes
}
